window.onload = (event) => {
    //Id для еды, для кнопок покупок
    // const Foods = Array.from(document.getElementsByClassName("Food"))
    // console.log(Foods)
    // Foods.forEach((element, index) => {
    //     element.id = 'Food-' + index
    // });

    // const ModalBtns = Array.from(document.getElementsByClassName("Food__Move-To-Modal"))
    // console.log(ModalBtns)
    // Foods.forEach((element, index) => {
    //     element.addEventListener('click', openModal())
    //     console.log(`Добавлен слушатель к ${index}`)
    // });

    // const ShopcartBtns = Array.from(document.getElementsByClassName("Food__Add-To-Shopcart-Button"))
    // console.log(ShopcartBtns)
    // Foods.forEach((element, index) => {
    //     element.id = 'ShopcartBtn-' + index
    // });

    console.log('Загружен модуль Ids')
}